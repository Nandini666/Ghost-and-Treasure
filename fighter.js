/*class fighter{
    constructor(x,y,width,height) {
        this.fighter1=loadImage("fighter shield pic.jpg");
      var options = {
          isStatic: true
      }
      this.body = Bodies.rectangle(x,y,width,height,options);
      this.width = width;
      this.height = height;
      World.add(world, this.body);
    }
    display(){
        this.body.position.x=mouseX;
        this.body.position.y=mouseY;
        image(this.fighter1,this.body.position.x,this.body.position.y,this.width,this.height);
    }
  }*/